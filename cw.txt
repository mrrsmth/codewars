7 kyu, 40+, 7/04-7/05
7/04: 
1. Fix My Phone Numbers!
2. Descending Order
3. Shortest Word
4. Beginner Series #3 Sum of Numbers
5. Two to One
6. Friend or Foe?
7. Regex validate PIN code
8/04:
8. Reverse words
9. Credit Card Mask
12/04
10. Sort array by string length
11. Round up to the next multiple of 5
13/04
12. The highest profit wins!
13. Find the stray number
14. Testing 1-2-3
15. Vowel Count
16. Sort Numbers
17. Anagram Detection
14/04
18. Remove anchor from URL
19. Number of People in the Bus
20. Make a function that does arithmetic!
21. No oddities here
22. C.Wars
23. Breaking chocolate problem
24. Two Oldest Ages
25. Two fighters, one winner.
18/04
26. Band name generator
27. Maximum Multiple
28. String ends with?
29. Small enough? - Beginner
30. Summing a number's digits
19/04
31. Are the numbers in order?
32. Fizz Buzz
33. 

<!-- <!DOCTYPE html>
<html>
   <head>
      <title>File Upload</title>

      <style type="text/css">
         body {
            margin: 0 auto;
            max-width: 40em;
            width: 88%;
         }

         label,
         input {
            display: block;
            width: 100%;
         }

         input {
            margin-bottom: 1em;
         }
      </style>
   </head>
   <body>
      <h1>File Upload</h1>

      <form id="upload">
         <label for="file">File to upload</label>
         <input type="file" id="file" accept=".json" />

         <button>Upload</button>
      </form>
      <div id="box"></div>
      <script>
         // Get the form and file field
         let form = document.querySelector('#upload');
         let file = document.querySelector('#file');
         let box = document.querySelector('#box');
         let data = [];
         /**
          * Log the uploaded file to the console
          * @param {event} Event The file loaded event
          */
         function logFile(event) {
            let str = event.target.result;
            let json = JSON.parse(str);
            // console.log('string', str);
            data.push(json);
            data.forEach((item) => {
               for (const key in item) {
                  const value = item[key];
               }
            });
            console.log(data);
            box.innerHTML = data;
            // console.log('json', json);
         }

         /**
          * Handle submit events
          * @param  {Event} event The event object
          */
         function handleSubmit(event) {
            // Stop the form from reloading the page
            event.preventDefault();

            // If there's no file, do nothing
            if (!file.value.length) return;
            console.log(file.value);
            // Create a new FileReader() object
            let reader = new FileReader();

            // Setup the callback event to run when the file is read
            reader.onload = logFile;

            // Read the file
            reader.readAsText(file.files[0]);
         }

         // Listen for submit events
         form.addEventListener('submit', handleSubmit);
      </script>
   </body>
</html> -->


